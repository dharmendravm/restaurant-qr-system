import Card from "../components/Card";

const HeroSection = () => {
  return (
    <div className="hero-bg h-screen flex items-center justify-center rounded-3xl p-10">
        <Card/>
    </div>
  );
};

export default HeroSection;